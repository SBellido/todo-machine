import React from "react";
import "./TodosError.css";

/**
 * Componente TodosError
 *
 * Este componente representa un mensaje de error al cargar los TODOs.
 *
 * Muestra un mensaje indicando al usuario que ocurrió un error al cargar los TODOs.
 */

function TodosError() {
  return <p>Error al cargar, vuelve a intentarlo.</p>;
}

export { TodosError };
